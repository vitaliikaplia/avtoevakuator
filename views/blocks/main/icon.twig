{% extends "block-base.twig" %}

{% block content %}

    {% if fields.icon and fields.color %}
    <figure class="icon" style="color: {{ fields.color }};">
        <svg>
            <use href="{{ svg_sprite }}#{{ fields.icon }}"></use>
        </svg>
    </figure>
    {% endif %}

{% endblock %}
